name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test -coverprofile=coverage.out ./...
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out
      - name: Build
        run: go build ./...
      - name: Build examples
        run: go build ./examples/...

  test-v2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: v2/go.mod
          cache: true
      # Force standalone module mode: remove go.work so v2 resolves
      # dependencies from its own go.mod, not the root workspace.
      - name: Force standalone v2 module
        run: |
          if [ -f go.work ]; then rm go.work; fi
          if [ -f go.work.sum ]; then rm go.work.sum; fi
      - name: Tidy v2
        working-directory: v2
        run: go mod tidy
      - name: Vet v2
        working-directory: v2
        run: go vet ./...
      - name: Test v2
        working-directory: v2
        run: go test -race -coverprofile=coverage-v2.out ./...
      - name: Upload v2 coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-v2
          path: v2/coverage-v2.out
      - name: Build v2
        working-directory: v2
        run: go build ./...
